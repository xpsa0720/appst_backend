services:
  postgres:
    image: postgres:17
    restart: always
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: xpsa
      POSTGRES_PASSWORD: Yong0021!
      POSTGRES_DB: appst
